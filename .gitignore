# Python environments
.venv/
env/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
serviceAccountKey.json
.DS_Store

.env

# --- Added 2026-08-23 -------------------------------------------------
# The OSRM graph, built locally by docker from the Egypt OSM extract.
# 2.6 GB, fully reproducible, and the single biggest reason a stray
# `git add .` in this folder would be very painful to undo.
osrm_data/

# Run artifacts and audit outputs. Regenerated by restart_and_test.ps1,
# rerun_audit.ps1, build_osrm_cache.py and build_audit_report.py -- keep
# the scripts in git, not the things they produce.
server.log
live_route.json
aastmt_route.json
route_debug.json
route_comparison_cache.json
route_comparison_cache.previous.json
barrier_sweep_results.json
*.xlsx
*.db

# One-off diagnostics kept around only while chasing a specific bug.
diag*.py

# Reconstructed feed backups and runtime coverage telemetry
gtfs_data.backup*
coverage_gaps.jsonl
osrm_car/
osrm_foot/

# NOT ignored on purpose:
#   gtfs_data/osrm_edge_cache.json -- 1,623 stop pairs of real OSRM road
#   distance, ~20 minutes to rebuild and needed for the engine to load
#   with corrected edges. It belongs in the repo.
